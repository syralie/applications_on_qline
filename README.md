# Applications on Qline

Here we are developing quantum communication protocols based on Quantum Key Distribution protocols, specifically [Qline](https://veriqloud.com/qline/) from VeriQloud.


# Methodology

To support application development on top of the Qline system, we designed a layered software stack that enables protocol communication and provides a testbed for future applications.
The stack consists of three layers, all of which must be deployed by both communicating parties in order to run applications.

- Hardware Layer
At the hardware layer, users may choose between the physical Qline device and a hardware simulator.
The simulator reproduces the behavior of Qline in a one-to-one manner.
Throughout this paper, we refer to the hardware simulator as **hwsim**.

- Global Counter Layer
The Global Counter layer is responsible for maintaining synchronization between the communicating parties.
It ensures that both sides consume correlated key material in a consistent order.
We refer to this layer as **gc**.

- Application Software Layer
The application software layer includes protocols built on top of QKD.
Currently, we support two applications: quantum oblivious transfer and memory-less quantum tokens.


## Workflow
At a high level, symmetric keys (or derived keys) are generated by the hardware layer and passed to the Global Counter layer.
The **gc** software running on both parties then writes the derived keys into local FIFO buffers in a synchronized manner.
This design guarantees that application-layer software can continuously read correlated data on both sides.

To provide greater flexibility for application development, we also expose a control API that allows applications to actively start or stop the key data stream from the lower layers.
Conceptually, this API acts as a data-stream switch, controlled via a classical communication channel.
By decoupling data generation from application consumption, this design enables fine-grained control over resource usage and supports dynamic application behavior without compromising synchronization guaranties.


# Usage


## with hwsim as the Hardware Layer

- Clone and build [hw_sim](https://github.com/Veriqloud/hw_sim), obtain the executable **sim**.

- Clone and build [gc](https://github.com/Veriqloud/kiwi_hw_control), obtain the executable **gc_alice** and **gc_bob**. 

- Generate config files by [kiwi_hw_control/config/gen_cinfig](https://github.com/Veriqloud/kiwi_hw_control)

- Run the hardware simulations and GC programs sequentially in the following order:
*simulation_bob → simulation_alice → gc_bob → gc_alice*, using the corresponding configuration files with arugent *-c*. For debugging purposes, we recommend running each command in a separate terminal.

For example, place the configuration files in kiwi_hw_control/config/sim/. Then execute the generated binaries:

```bash
sim -c bob/sim.json
sim -c alice/sim.json
gc_bob -c bob/gc.json
gc_alice -c alice/gc.json
```

- At this point we have everything rinning below the application layer. Execute the protocol programs by following the instructions provided in the README of the selected protocol subfolder.



## with real Qline hardware as the Hardware Layer

- Initialize Qline system, after automatic initialization, the system should activate in both hardware and gc layers.

- Execute the protocol programs by following the instructions provided in the README of the selected protocol subfolder.





